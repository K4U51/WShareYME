# WShareYME
```
# LVGL Image Mapping Guide
# ------------------------
# Original PNG file → Converted C file → LVGL Symbol Name → Usage Example

g_img1.png    → g_img1.c    → ui_img_g_img1_png    → lv_img_set_src(g_img1, &ui_img_g_img1_png);
g_img2.png    → g_img2.c    → ui_img_g_img2_png    → lv_img_set_src(g_img2, &ui_img_g_img2_png);
g_img3.png    → g_img3.c    → ui_img_g_img3_png    → lv_img_set_src(g_img3, &ui_img_g_img3_png);

# Optional descriptive names (more readable)
bg1.png       → bg1.c       → ui_img_bg1_png       → lv_img_set_src(g_img1, &ui_img_bg1_png);
bg2.png       → bg2.c       → ui_img_bg2_png       → lv_img_set_src(g_img2, &ui_img_bg2_png);
overlay.png   → overlay.c   → ui_img_overlay_png   → lv_img_set_src(g_img3, &ui_img_overlay_png);

# Notes:
# 1. LVGL symbol names are derived from the PNG filename, prefixed with 'ui_img_' and suffixed with '_png'.
# 2. The .c file generated by the converter may match the original PNG name but you always use the symbol in code.
# 3. Keep filenames lowercase and without spaces for safe conversion.
# 4. Use lv_img_set_src() with the & symbol pointing to the declared LVGL image.

# LVGL Image Integration Checklist
# -------------------------------

# 1️⃣ LVGL Image Declarations
# File: LVGL_Example.h
# Lines to add/check:
#   LV_IMG_DECLARE(ui_img_g_img1_png);
#   LV_IMG_DECLARE(ui_img_g_img2_png);
#   LV_IMG_DECLARE(ui_img_g_img3_png);
# Notes: Add under other includes or after driver includes.

# 2️⃣ LVGL Image Usage
# File: LVGL_Example.cpp
# Lines to add/check (example locations):
#   lv_img_set_src(g_img1, &ui_img_g_img1_png);   # usually in setup function or screen create function
#   lv_img_set_src(g_img2, &ui_img_g_img2_png);
#   lv_img_set_src(g_img3, &ui_img_g_img3_png);
```
```
# 3️⃣ Image .c Files
# Folder: /src or /LVGL_Images (your choice)
# Files to include in repo and compile:
#   g_img1.c
#   g_img2.c
#   g_img3.c
# Notes:
#   - The LVGL converter produces these.
#   - The symbol inside is what is used in code (ui_img_<filename>_png).
#   - Keep filenames lowercase and without spaces.

# 4️⃣ Optional renaming for clarity (recommended)
#   bg1.png → bg1.c → ui_img_bg1_png → g_img1
#   bg2.png → bg2.c → ui_img_bg2_png → g_img2
#   overlay.png → overlay.c → ui_img_overlay_png → g_img3

# 5️⃣ Include order in main.cpp / setup
#   #include "LVGL_Example.h"  # after LVGL_Driver.h
#   LVGL images are declared via LV_IMG_DECLARE
#   Use lv_img_set_src() after Lvgl_Init() and inside screen creation functions.

# ✅ Notes for verification:
#   - Check that LV_IMG_DECLARE symbol names match exactly what LVGL converter generated.
#   - Make sure the .c files are compiled in the project.
#   - Ensure lv_img objects (g_img1, g_img2, g_img3) exist before calling lv_img_set_src().
